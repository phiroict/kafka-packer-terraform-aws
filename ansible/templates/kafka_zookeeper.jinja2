#!/usr/bin/env bash
{{ hostvars['localhost'].broker_id | int + 1  }}
sudo zookeeper_config -E -S -i {{ hostvars['localhost'].broker_id }} -m {{zookeeper_memory}}m -n {{zookeepers_sequence}}
sudo kafka_config -a {{hostvars[inventory_hostname]['ansible_default_ipv4']['address']}} -E -i {{ hostvars['localhost'].broker_id }} -m {{kafka_mem}}m -S -z {{kafka_zoo}}
